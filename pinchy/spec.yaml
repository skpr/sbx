schemaVersion: "1"
kind: sandbox
name: pinchy
displayName: pinchy
description: Custom sandbox environment for the Pinchy state of mind.

agentContext: |
  You are running inside a herdr-managed sandbox. Multiple coding agents may
  run in parallel panes. Keep changes scoped to the mounted workspace.

sandbox:
  image: ghcr.io/skpr/sbx:pinchy-latest

  aiFilename: AGENTS.md
